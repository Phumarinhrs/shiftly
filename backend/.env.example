# Local Postgres (recommended: docker compose up -d postgres)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ppjshift"

# JWT (ใช้ค่า random ก็ได้ — สำหรับ dev)
JWT_SECRET="dev-secret-change-in-production"
JWT_EXPIRES_IN="7d"

# Server
PORT=3000
